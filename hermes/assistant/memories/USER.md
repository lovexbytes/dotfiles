User wants true local computer-use on their Mac, especially control of their own browser window, rather than a sandboxed/remote browser session.
§
User expects the agent to distinguish between `python` missing on PATH and Python being installed as `python3`, and to avoid saying Python is not installed when only the `python` shim is absent.