The Hako repository lives at ~/Documents/dev/hako and uses repo-local OpenCode skills under .opencode/skills/.
§
In this execution environment, Python is available as /usr/bin/python3, while the `python` command is not on PATH.