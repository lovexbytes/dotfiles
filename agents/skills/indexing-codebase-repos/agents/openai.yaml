interface:
  display_name: "Repo Index Builder"
  short_description: "Build and audit repo navigation indexes"
  default_prompt: "Use $indexing-codebase-repos to build or refresh a repo docs/index.md map."
